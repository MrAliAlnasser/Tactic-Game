# مستند سياق المشروع (Project Context)
**اسم المشروع:** القرار الأخير (The Last Decision)  
**النوع:** لعبة استراتيجية حربية (تعتمد على الأدوار - Turn-based Strategy)  

يُعد هذا المستند **الدليل المرجعي الصارم والعقل الثاني للتطوير** وفهم معمارية الكود الحالي، الأنظمة الأساسية، وأسلوب البرمجة والتصميم المتبع في اللعبة. صُمم هذا المستند ليكون مرجعاً لأي ذكاء اصطناعي أو مطور يكمل العمل على المشروع لضمان التوافق التام مع الهيكلية الحالية وتجنب كسر الأنظمة أو الأغلاط المنطقية.

---

## 1. نظرة عامة على المشروع (Project Overview)
- **وصف اللعبة:** أنت قائد مطار حربي. هدفك هو إدارة الموارد، استشارة الجنرالات، واستكشاف خريطة المعركة للبحث عن قاعدة العدو وتدميرها قبل أن يتم تدمير مطارك.
- **المحرك التقني:** لا يوجد محرك ألعاب خارجي (No Game Engine). اللعبة مبنية بالكامل باستخدام (Vanilla JavaScript, HTML5 Canvas, CSS3).
- **الواجهة والرسوميات:** تعتمد اللعبة على تقنية الـ DOM للواجهة الرسومية (UI)، وتستخدم عنصر الـ Canvas (`<canvas>`) المدمج في HTML5 لرسم المشاهد الحركية برسوميات متجهة عالية الدقة ثلاثية الأبعاد برمجياً (Procedural Pseudo-3D Vector Graphics).

---

## 2. الهيكل المعماري والروابط المنطقية (Architecture & Logical Flow)
يعتمد المشروع على **معمارية الحالة المركزية (Centralized State Architecture)**، حيث يتم التحكم باللعبة من خلال متغير عام يجمع كل بيانات الجلسة الحالية، وتتواصل الأنظمة عبر وظائف (Functions) تعدل على هذه الحالة ثم تحدّث واجهة المستخدم.

- **كائن الحالة `G` (Global State):** هو قلب اللعبة، ويحتوي على كافة المتغيرات مثل `turn` (رقم الدور)، `health` (صحة المطار)، `resources` (الموارد)، `intel` (المعلومات)، `enemyPos` (موقع العدو)، `hexCoords` (إحداثيات الخريطة السداسية)، وحالة الترقيات `upgrades`.
- **دورة اللعب (Game Loop):**
  1. تبدأ باختيار اللاعب لأحد الجنرالات (`selectGeneral`).
  2. يتم عرض المشورة (Advice) وتحديد التكلفة (Cost).
  3. عند التنفيذ، يتم استدعاء `playExecutionSequence` والذي يعرض شاشة التحميل/الرسوميات، ثم يُستدعى `executeChoice` لتطبيق التأثير (مثل الكشف عن الخريطة `executeScout` أو الهجوم `executeStrike`).
  4. يُستدعى `endPlayerTurn` لإنهاء دور اللاعب وحساب أي تأثيرات تراكمية وتنفيذ الإصلاحات الهندسية.
  5. يُستدعى `enemyTurn` لكي يقوم النظام بحساب احتمالية هجوم العدو وتطبيق الضرر على القاعدة أو اعتراض الدفاع الجوي.
  6. يتم تحديث الواجهة من خلال استدعاء دالة `updateUI()` لتعكس الأرقام والأصوات والحالات الجديدة.
- **التفاعل مع الواجهة:** جميع النوافذ المنبثقة (Modals) تدار بأسلوب إظهار وإخفاء أصناف CSS (`classList.toggle('active')`).

---

## 3. آليات اللعبة الأساسية وقواعد المنطق المحمية (Core Mechanics & Logical Rules)

### أ. نظام الخريطة والجوار السداسي (Hexagonal Grid & Adjacency)
- الخريطة تنشأ بنظام سداسي من 16 خلية باستخدام إحداثيات المحاور `{q, r}` المحفوظة في مصفوفة `G.hexCoords`.
- **حساب الجوار في دالة `isAdjacent(a, b)`:** يتم حسابه حصراً بالعتاد السداسي (Hex Axial Distance):
  $$\text{Distance} = \frac{|q_1 - q_2| + |r_1 - r_2| + |(q_1 + r_1) - (q_2 + r_2)|}{2}$$
  يُمنع استخدام أي معادلة جوار مربعة 4×4 لضمان تطابق تلميحات الاستطلاع مع الشبكة البصرية المضلعة.

### ب. نظام المعلومات وتزامن الكشف الفوري (Instant Intel Discovery)
- عند وصول نقاط `G.intel` إلى 10 (عبر `executeGatherIntel` أو مساعدات الحلفاء أو الاستطلاع):
  - يتم تعيين `G.isEnemyFound = true` و `G.map[G.enemyPos] = 2` فوراً.
  - يُمنح تروفي `eagle_eye`.
  - يتم تبديل الموسيقى فوراً إلى `Chronicles_of_the_Ascendant` دون انتظار الدور التالي.

### ج. ترقية رادار الكشف المبكر (`G.upgrades.radar`)
- **الوصف التوضيحي:** يمنح `+1` نقاط معلومات إضافية مع كل عملية جمع استخباراتي.
- **الربط المنطقي:** عند تنفيذ `executeGatherIntel()` ترتفع نقاط المعلومات بمقدار `3` بدلاً من `2` وتظهر في التقرير `(+1 بفضل رادار الكشف المبكر)`.
- **الحركة البرمجية 3D:** يدور صحن الرادار أفقياً بـ 360 درجة حول المحور الرأسي (Yaw Rotation) مع إبراز الوجه المقعر الأمامي والهيكل الشبكي الخلفي.

---

## 4. الرسوميات البيئية ثلاثية الأبعاد برمجياً (Procedural 3D Environment Graphics)
- **يُمنع استخدام البكسلات المربعة (Pixel Art) أو استدعاء صور حقيقية خارجية** لشاشات البيئة والمطار والمباني.
- **الجبال ثلاثية الأبعاد (`drawProcedural3DMountains`):** سلاسل منحنية تراكمية بتدرجات 3D وحواف مضاءة وضباب أفق مدمج.
- **مباني المطار ثلاثية الأبعاد (`drawProcedural3DHangar`):** أسقف مقببة، جدران منظور ببعد ثالث، وبوابات تجويفية بإنارة مخروطية.
- **برج المراقبة ورادار الكشف المبكر (`drawProcedural3DControlTower`):** عمود الخرسانة الـ 3D مع الكبينة الزجاجية، وصحن رادار يدور أفقياً بحسابات الـ Projection الجيبية.

---

## 5. قواعد نظام الصوتيات والموسيقى (Audio & Music Rules)
تعتمد اللعبة على مدير صوتيات مخصص (`const SFX`) باستخدام `Web Audio API` ومقطوعات BGM محددة حسب الحالة:
1. **بداية اللعبة:** موسيقى `March_Toward_the_Iron_Gates`.
2. **اكتشاف قاعدة العدو:** عند `G.isEnemyFound = true` يتم الانتقال فوراً إلى `Chronicles_of_the_Ascendant`.
3. **إنذار الخطر:** عند كشف العدو للمطار قبل كشف قاعدته (`G.redAlarmTriggered = true`) يتم الانتقال إلى `Red-Alarm`.
4. **تخطي الإنذار:** عند كشف قاعدة العدو بعد إنذار الخطر، يتم الانتقال مباشرة إلى `Chronicles_of_the_Ascendant`.
5. **منع التداخل:** يجب إيقاف المقطوعة الحالية وتصفير `currentTime` قبل تغيير المصدر وتشغيل المقطوعة التالية.

---

## 6. قواعد الصرامة ومنع الهلوسة للذكاء الاصطناعي (Strict AI Rules)
1. **الالتزام بالنطاق المطلوب (Strict Scoping):** نفّذ التغييرات المطلوبة فقط دون إضافة أزرار أو ميزات جانبيّة غير مطلوبة.
2. **منع إعادة كتابة الكود (No Unprompted Rewrites):** يُمنع إعادة هيكلة الدوال أو تغيير أسلوب البرمجة الإجرائية المعتمد.
3. **التوافق مع الحالة المركزية:** كل المتغيرات تُحفظ داخل كائن `G` حصراً، مع استدعاء `updateUI()` في نهاية التغييرات الرئيسية.
4. **منع التبعيات والمكتبات (Zero Dependencies):** تعتمد اللعبة كلياً على (Vanilla JS, HTML, CSS) دون أي مكتبات خارجية.
5. **الالتزام بالقواعد البصرية:** المحافظة على التنسيقات الأنيقة لكروت الجنرالات والرسوميات ثلاثية الأبعاد البرمجية.
