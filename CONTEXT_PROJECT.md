# مستند سياق المشروع (Project Context)
**اسم المشروع:** القرار الأخير (The Last Decision)
**النوع:** لعبة استراتيجية حربية (تعتمد على الأدوار - Turn-based Strategy)

يُعد هذا المستند دليلاً شاملاً لفهم معمارية الكود الحالي، الأنظمة الأساسية، وأسلوب البرمجة المتبع في اللعبة. صُمم هذا المستند ليكون مرجعاً لأي ذكاء اصطناعي أو مطور سيكمل العمل على المشروع لضمان التوافق مع الهيكلية الحالية وتجنب كسر الأنظمة.

---

## 1. نظرة عامة على المشروع (Project Overview)
- **وصف اللعبة:** أنت قائد مطار حربي. هدفك هو إدارة الموارد، استشارة الجنرالات، واستكشاف خريطة المعركة للبحث عن قاعدة العدو وتدميرها قبل أن يتم تدمير مطارك.
- **المحرك التقني:** لا يوجد محرك ألعاب خارجي (No Game Engine). اللعبة مبنية بالكامل باستخدام (Vanilla JavaScript, HTML, CSS).
- **الواجهة والرسوميات:** تعتمد اللعبة على تقنية الـ DOM للواجهة الرسومية (UI)، وتستخدم عنصر الـ Canvas (`<canvas>`) المدمج في HTML5 لرسم المشاهد الحركية والـ Pixel Art (مثل مشهد إقلاع الطائرات أو القصف).

---

## 2. الهيكل المعماري والروابط المنطقية (Architecture & Logical Flow)
يعتمد المشروع على **معمارية الحالة المركزية (Centralized State Architecture)**، حيث يتم التحكم باللعبة من خلال متغير عام يجمع كل بيانات الجلسة الحالية، وتتواصل الأنظمة عبر وظائف (Functions) تعدل على هذه الحالة ثم تحدث واجهة المستخدم.

- **كائن الحالة `G` (Global State):** هو قلب اللعبة، ويحتوي على كافة المتغيرات مثل `turn` (رقم الدور)، `health` (صحة المطار)، `resources` (الموارد)، `intel` (المعلومات)، `enemyPos` (موقع العدو)، وحالة الترقيات `upgrades`.
- **دورة اللعب (Game Loop):**
  1. تبدأ باختيار اللاعب لأحد الجنرالات (`selectGeneral`).
  2. يتم عرض المشورة (Advice) وتحديد التكلفة (Cost).
  3. عند التنفيذ، يتم استدعاء `playExecutionSequence` والذي يعرض شاشة التحميل/الرسوميات، ثم يُستدعى `executeChoice` لتطبيق التأثير (مثل الكشف عن الخريطة `executeScout` أو الهجوم `executeStrike`).
  4. يُستدعى `endPlayerTurn` لإنهاء دور اللاعب وحساب أي تأثيرات تراكمية.
  5. يُستدعى `enemyTurn` لكي يقوم النظام بحساب احتمالية هجوم العدو وتطبيق الضرر على القاعدة.
  6. يتم تحديث الواجهة من خلال استدعاء دالة `updateUI` لتعكس الأرقام الجديدة.
- **التفاعل مع الواجهة:** جميع النوافذ المنبثقة (Modals) تدار بأسلوب إظهار وإخفاء أصناف CSS (`classList.toggle('active')`).

---

## 3. آليات اللعبة الأساسية (Core Mechanics)
من خلال الكود، تم بناء واكتمال عدة أنظمة متداخلة:
- **نظام الخريطة والاستكشاف:** الخريطة عبارة عن مصفوفة من 16 مربعاً (`G.map`). يمكن للاعب إرسال طائرات استطلاع (`executeScout`) لكشف المربعات. يتخفى العدو في موقع عشوائي (`G.enemyPos`).
- **نظام المعلومات (Intel System):** خيار آخر لمعرفة مكان العدو هو جمع المعلومات (`executeGatherIntel`). عند وصول نقاط `G.intel` إلى 10، يتم الكشف عن موقع العدو تلقائياً.
- **نظام الصحة والترقيات (Health & Upgrades):** 
  - يمتلك المطار نقاط صحة (`G.health`).
  - يمكن إنفاق الموارد (`G.resources`) في شاشة التطوير (`Consultation Room / RnD`) لشراء ترقيات مثل (الرادار، الجدران، الدفاع الجوي، الطيران الشبحي).
- **ذكاء العدو (Enemy AI & Aggression):** يعتمد العدو على احتمالات رياضية. ترتفع احتمالية هجوم العدو تدريجياً مع تقدم الأدوار (`attackChance = 0.35 + G.turn * 0.02`). إذا تم كشف موقع اللاعب (`G.enemyKnowsUs = true`)، يصبح العدو أكثر شراسة ودقة في قصف المطار وقد يدمر الترقيات المشتراة.
- **نظام الحلفاء (Ally Support):** بين الحين والآخر، تتدخل دالة `offerAllySupport` لتقديم خيارات مساعدة خارجية للاعب (مثل ترميم القاعدة، تقديم موارد، أو درع حماية مؤقت).

---

## 4. قواعد كتابة الكود المستنتجة (Coding Conventions)
- **نمط التسمية (Naming Convention):** 
  - المتغيرات والدوال تستخدم أسلوب **CamelCase** (مثل `enemyKnowsUs`, `updateUI`, `startTurn`).
  - الثوابت العامة وإعدادات اللعبة تستخدم أحرفاً كبيرة (UPPERCASE) مثل `TROPHIES`, `GENERALS`, `STORY`.
- **النموذج البرمجي (Programming Paradigm):** 
  - المشروع **لا يعتمد** على البرمجة الكائنية التوجه (OOP) بالمعنى الكلاسيكي (لا يوجد `class Player` أو `class Enemy`). 
  - يعتمد على **البرمجة الإجرائية (Procedural Programming)** وهيكلة تعتمد على دوال بحتة (Functions) تتلاعب في كائن حالة شامل (State Object).
- **أنماط التصميم (Design Patterns):** 
  - اللعبة تستخدم بشكل صريح ما يشبه نمط **Singleton / Module Pattern** في إدارة الحالة (كائن `G` وكائن `SFX` للصوتيات).

---

## 5. المكتبات والتبعيات (Libraries & Dependencies)
- اللعبة **مستقلة تماماً (Dependency-Free)** من ناحية المنطق والواجهة. لا توجد مكتبات مثل React, Vue, أو jQuery.
- **الصوتيات:** تم بناء مدير صوتيات مخصص (`const SFX`) يعتمد على مكتبة الواجهة البرمجية الأصلية للمتصفح `Web Audio API` (حيث يظهر استخدام `ctx.createGain()`).
- **الخطوط:** يتم جلب خط "Thmanyah" من خوادم Framer خارجية عبر الـ CSS `@font-face`.

---

## 6. قواعد نظام الصوتيات والموسيقى (Audio & Music Rules)
تمتلك اللعبة نظاماً معقداً للموسيقى التصويرية يعتمد على حالة اللعب الحالية (Game State) لضمان التجربة الدرامية، ويجب الالتزام بالقواعد التالية عند أي تعديل:
1. **بداية اللعبة:** تبدأ اللعبة دائماً بموسيقى `March_Toward_the_Iron_Gates`.
2. **اكتشاف قاعدة العدو:** عند اكتشاف اللاعب لموقع قاعدة العدو (`G.isEnemyFound = true`)، يجب الانتقال فوراً إلى مقطوعة `Chronicles_of_the_Ascendant`.
3. **إنذار الخطر:** عند كشف العدو لموقع اللاعب قبل أن يكتشف اللاعب موقع العدو (`G.redAlarmTriggered = true`)، يتم الانتقال إلى موسيقى `Red-Alarm`.
4. **تخطي الإنذار:** إذا كان إنذار الخطر (`Red-Alarm`) يعمل، ثم نجح اللاعب لاحقاً في اكتشاف موقع قاعدة العدو، يجب الانتقال مباشرة إلى `Chronicles_of_the_Ascendant`.
5. **منع التداخل:** لا يجوز إطلاقاً تشغيل أكثر من مقطوعة موسيقية (BGM) واحدة في الوقت نفسه. يجب إيقاف المقطوعة الحالية بالكامل (عبر `pause()` وتصفير `currentTime`) قبل تغيير المصدر وتشغيل المقطوعة التالية.

---

**ملاحظة للمطور/الذكاء الاصطناعي اللاحق:** عند إضافة أي ميزة جديدة، تجنب إنشاء فئات (Classes) معقدة أو استخدام مكتبات خارجية لتجنب كسر التناسق. قم بإضافة المتغيرات الجديدة داخل كائن `G`، وأضف الدوال الإجرائية المطلوبة مع الالتزام بتحديث الواجهة عبر `updateUI()` في نهاية كل عملية.
